// app_main.cpp — application entrypoint.
//
// The Coordinator will be constructed and started here in a future PR.
// For now this is a minimal placeholder that satisfies the build system
// and establishes the C++ entrypoint convention.

extern "C" void app_main(void)
{
    // TODO(PR-next): construct Coordinator and start the runtime.
}
